<div id='blogs'>
  <h2>Blogs</h2>
    <% @blogs.each_with_index do |blog, index| %>
      <div class="blog-box">
        <h3><%= blog.title %></h3>
        <div>
          <%= blog.body.html_safe %>
        </div>
      </div>
    <%end%>

</div>
